# Done — CV22.DS10.US1

## Status

Done

## History Action

Four commits on main: 5e9b9d2a (the console process and check_retired_surfaces.py), 48101788 (the six web-only read models and the scene surface), 8013db74 (docs, cutoff, D-017 correction, CI wiring), plus a closure commit carrying the lifecycle artifacts, CR090, the D-005 and eval-gate updates, the roadmap status, and the worklog. No push or release.

## Roadmap Update

Story index reads Done with its outcome; the DS10 candidate row reads Done and names the two layers the gate did not; the retirement gate's six items are marked satisfied; DS10 reads 2/8; the CV22 row names TS1 and US1. The cutoff is staged in pending-cutoffs.md; D-005 now records that routing is the sole red and points at TS3; the eval gate carries the eval-history disposition question.

## Next Recommendation

Pull CV22.DS10.TS2 — extension compatibility-host deletion — the next story by the package's ordering, and the one that also owns the Extension API version question D-018 left for it after TS1 removed api.journey_projections. Two items travel with it: CR090 (the Portuguese in the Debt Review surface, both engines) and CR089 (the journey route mis-route, which should land before TS4 deletes the Python behind it).

## Missing Done

- none
