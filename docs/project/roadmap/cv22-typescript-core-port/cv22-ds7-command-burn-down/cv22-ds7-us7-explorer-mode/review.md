# Review — CV22.DS7.US7

## Status

Pending debt decision

## Debt Findings

- Six findings. Paying one now, deferring five. PAY NOW -- CR071 (RS010), the triplicated skill command references: US7 rewrote 12 explore invocations across three copies by search-and-replace, the third story to pay that tax after US6's 20-per-copy soul rewrite, and US8 carries the largest skill surface in the product. A missed copy fails no test; it silently leaves one runtime calling Python after a flip, which is CR059 in a different costume. DEFER to RS010 with revisit trigger 'before US8 is pulled': (2) every TS-routed explore leaf, including read-only story show, snapshots the database to a FIXED filename before answering, so a read overwrites the last-write undo point -- introduced by US6 for soul listen, widened by eleven leaves here; (3) tests/unit/memory/web/test_server.py::test_operations_run_api_executes_runtime_diagnose_through_controlled_command allows 2.00s for a subprocess measured at 2.29-2.87s locally, a budget below its own floor that happens to fit CI's runners; (4) the delegated journey-projection refresh subcommand is a contract TS now depends on but sits outside the oracle-drift tripwire, guarded only by the lifecycle smoke's published-file check; (5) the obfuscator still misses /home/... paths and bearer tokens without an '=' (the plan's original claim that phone numbers were missing was wrong and was corrected at plateau 3); (6) the decision record's claim that 'every Explorer Story write publishes into the user's project' is wrong -- the live session showed publication happens once and then only when a durable Builder handoff exists, because refresh.py projects only stories that carry one. The fcntl.flock reasoning behind the decision is untouched; the frequency claim is not.

## Debt Decision

pay_now

## Defer Reason

none

## Revisit Trigger

none

## Missing Decision

- pay-now debt must route through Refactor before Done
