# Coherence — CV22.DS7.US7

## Status

Coherent

## Process Alignment

Ariad lifecycle walked in order with two honest interruptions recorded rather than smoothed over: a scope_change_detected stop at plateau 2 that produced the projection-seam decision, and a same-session reversal of that decision when the subsystem's fcntl.flock contract showed an early port would create an unsynchronized dual writer. Both the superseded decision and its reversal are kept in docs/project/decisions.md, because the first one's evidence is why the seam exists at all. Plan carries a Navigator-authorized Scope Amendment (items 16-19) in the shape US6 used. Debt Review's pay_now was honoured before closure: CR071 is done, validated, and closed file-first in the canonical Refinement index with Driver and Delivery provenance preserved.

## Project Alignment

Roadmap, ledger, and story package agree. DS7 index: US7 Done, 11/15 stories, TS5 split out of TS4 and placed last in the ops tail with its reason. Burn-down ledger: Explorer Mode 1/1, a per-leaf table showing 11 of 14 leaves on TS with story promote blocked on US8 by name, an eleven-point flip checklist, and the void first validation run recorded because it is the failure class the ledger exists to catch. DS10 carries two new gates: the Journey Projection Refresh Seam and the Skill Invocation Gate. Oracle-drift tripwire registers four explore oracles. Doc links and skill command parity both clean. CI green at b8c9c2d on Tests and Docs.

## Product Alignment

Explorer Mode answers from TypeScript in live sessions on all three runtimes. Navigator validated nine populated surfaces across three real journeys pre-flip, byte-identical on both engines including the promoted-story path where an inactive legacy payload must not be resurrected, then ran a live Pi exploration post-flip: six commands, all explore/ts/exit=0, no fell_back, one durable row inheriting its id across four mutations, and a real attractor with accents and em-dashes round-tripping intact. The projection published once and then correctly answered unchanged. MIRROR_TS_EXPLORE=0 reverts the family with no code change. story promote still answers from Python by name until US8 owns Builder load.

## Local Guide Differences

- none

## Missing Coherence

- none
