---
name: ext-declared
description: Fixture extension exercising the declared mirror-cli-v1 command contract
---

# ext-declared

Not a real capability. Each subcommand exists twice — declared for TypeScript,
registered for Python — so one recorded corpus grades both paths.
