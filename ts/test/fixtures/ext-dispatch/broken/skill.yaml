id: broken
name: Broken Extension
category: extension
kind: command-skill
summary: A valid manifest whose register(api) raises on every load
table_prefix: ext_broken_

entrypoint:
  module: extension
  function: register

runtimes:
  pi:
    command_name: ext-broken
    skill_file: SKILL.md
