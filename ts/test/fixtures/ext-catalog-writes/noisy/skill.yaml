id: noisy
name: Noisy
category: extension
summary: Fixture whose source tree carries everything install must refuse to copy
kind: command-skill
table_prefix: ext_noisy_

entrypoint:
  module: extension
  function: register

runtimes:
  pi:
    command_name: ext-noisy
    skill_file: SKILL.md
